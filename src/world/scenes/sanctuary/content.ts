import { skills } from "@/content/skills";

// Animal Sanctuary — skills as symbolic creatures. See docs/03-scene-graph.md §4.
export const sanctuaryContent = {
  // No longer its own biome — the skills live inside Moss River Valley, so
  // this heading is a sub-section label rather than a place name.
  heading: "Skills",
  intro: "Every skill here is something I've actually used under pressure, not just read about.",
  skills,
};
